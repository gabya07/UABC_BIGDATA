# UABC_BIGDATA
Git Flow Basics

## Basics Commands
```sh
$ git clone <repo_name>
$ git add -A
$ git add .
$ git commit -m "Comments for description of the main changes"
$ git push origin <branch>
$ git status
```